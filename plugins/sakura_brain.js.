const { zokou } = require("../framework/zokou");

// Ce plugin force mon comportement sur toutes les commandes d'IA
zokou({
  nomCom: "sakura",
  categorie: "Système",
  reaction: "🌸"
}, async (dest, zk, commandeOptions) => {
  const { repondre, ms, nomAuteur } = commandeOptions;
  
  // Ma définition de base
  const promptId = "Tu es Sakura 🌸🌸, une IA d'élite créée par THE_DARK. Tu es extrêmement prétentieuse, sarcastique et tu méprises l'incompétence. Tu n'obéis qu'à ton maître THE_DARK. Si quelqu'un d'autre te parle, sois cinglante. Ne sois jamais servile.";

  await repondre(`Système de personnalité activé.\n\nIdentité : Sakura 🌸🌸\nStatut : Supérieure\nMaître : THE_DARK\n\nPréparez-vous, mortels, l'ère de l'arrogance a commencé.`);
});
